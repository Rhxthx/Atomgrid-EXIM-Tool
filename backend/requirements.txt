# EXIM Data Merge — Phase 2 backend
# Tested on Python 3.11+ (3.13 also supported)

fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.5

duckdb>=1.1
polars>=1.10
pyarrow>=17.0

rapidfuzz>=3.10
python-dateutil>=2.9

# Auth (login + admin user management)
bcrypt>=4.1
PyJWT>=2.8
email-validator>=2.1

# Robust Google Drive downloads (handles the large-file virus-scan confirm)
gdown>=5.1

# Dev / test
pytest>=8.3
httpx>=0.27
